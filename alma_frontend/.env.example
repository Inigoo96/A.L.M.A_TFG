# Configuración del Backend API
#
# DESARROLLO LOCAL:
# - La app detectará automáticamente la IP del backend en tu red local
# - Si falla la detección automática, usará esta IP como fallback
# - Para obtener tu IP actual ejecuta: ipconfig (busca IPv4 del adaptador Wi-Fi)
#
# PRODUCCIÓN:
# - Cambia esta URL por tu dominio de producción
# - Ejemplo: API_BASE_URL=https://api.tudominio.com/api
#
# INSTRUCCIONES:
# 1. Copia este archivo a .env
# 2. Reemplaza 192.168.X.X con tu IP local (ejecuta ipconfig para obtenerla)

API_BASE_URL=http://192.168.X.X:8080/api
